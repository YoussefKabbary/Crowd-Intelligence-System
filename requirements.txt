# Core AI / Detection
ultralytics>=8.0.0
torch>=2.0.0
torchvision>=0.15.0

# Vision & Display
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# Optional - REST API
fastapi>=0.100.0
uvicorn>=0.23.0

# Optional - PDF reports
reportlab>=4.0.0

# Standard library (no install needed):
# tkinter, threading, queue, json, csv, subprocess, logging, math, pickle
